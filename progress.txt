## Codebase Patterns
- Project uses Next.js 16 (App Router) with TypeScript strict mode
- Prisma 7 requires driver adapters (using @prisma/adapter-pg) - no direct DB URL in PrismaClient constructor
- Prisma client is generated to `src/generated/prisma/` - import from `@/generated/prisma/client`
- Tailwind v4 with `@theme inline` for custom tokens (not tailwind.config.js)
- ESLint 9 flat config with eslint-config-next + eslint-config-prettier
- Global prisma singleton in `src/lib/prisma.ts` using adapter pattern
- Prisma 7 seed config goes in `migrations.seed` (not top-level `seed`): `migrations: { seed: "npx tsx prisma/seed.ts" }`
- Without a running DB, use `prisma migrate diff --from-empty --to-schema prisma/schema.prisma --script` to generate SQL
- Seed script must create its own PrismaClient with adapter (can't use global singleton since it runs standalone)
- Auth: NextAuth v4 config in `src/lib/auth.ts`, route handler in `src/app/api/auth/[...nextauth]/route.ts`
- Auth middleware: `extractAuth(req)` in `src/lib/auth-middleware.ts` supports JWT and X-API-Key
- Protected routes: wrap handlers with `withAuth()` from `src/lib/api-handler.ts` to inject `tenantId`
- API keys stored as SHA-256 hashes; use `hashApiKey()` from auth-middleware
- RBAC: `withTenant()` and `withRole()` wrappers in `src/lib/rbac.ts` for tenant scoping + role checks
- Tenant scoping: use `withTenantFilter(tenantId, { ...where })` to add tenantId to Prisma queries
- Role hierarchy: ADMIN > SENIOR_UNDERWRITER > UNDERWRITER (hierarchy); SIU_INVESTIGATOR, COMPLIANCE_OFFICER (independent); BROKER (restricted)
- BROKER role is auto-blocked from non-portal API paths by `withTenant` middleware
- S3 client: `src/lib/s3.ts` with `uploadToS3(tenantId, submissionId, documentId, fileName, buffer, contentType)` returning the s3Key
- S3 key path format: `{tenantId}/{submissionId}/{documentId}/{filename}`
- Multipart uploads: use `req.formData()` in App Router, iterate entries with `instanceof File` check
- pdf-parse v2 uses class-based API: `new PDFParse({ data: Uint8Array })` then `.getText()` then `.destroy()`
- S3 downloads: `getFromS3(s3Key)` in `src/lib/s3.ts` returns Buffer
- Classification service: `src/services/classification.ts` with `classifyDocument(documentId)` function
- Extraction services: `src/services/extraction-acord125.ts` pattern — read doc from DB, extract PDF text via `extractPdfText(s3Key)`, regex-extract fields, store in `document.extractedData` JSON, set status to ANALYZED
- Prisma JSON fields: use `JSON.parse(JSON.stringify(data))` to satisfy `InputJsonValue` type when writing complex objects to Json columns
- Cross-doc validation pattern: `src/services/validation-cross-doc.ts` — use `findExtractedData<T>(submissionId, docType)` helper, compare values, create FraudIndicator with category CROSS_DOC
- Prisma nullable JSON where: don't use `{ not: null }` (type error), filter without it and check `!doc.extractedData` at runtime
- pdf-parse v2 metadata: use `getInfo()` → `InfoResult` with `.info`, `.total`, `.getDateNode()` for parsed dates; `getText()` → `TextResult` with `.text`
- Forensic services: fetch document with `include: { submission: true }`, create FraudIndicator with category FORENSIC per check
- Detection services pattern: `src/services/detection-statistical.ts` — main function fetches submission for tenantId, delegates to parallel check functions via Promise.all, each creates FraudIndicator with relevant category
- Industry benchmarks: `src/services/industry-benchmarks.ts` — `getBenchmark(naicsCode)` returns COGS margin range, payroll-per-head range, max revenue growth by 2-digit NAICS sector code
- Temporal detection: `src/services/detection-temporal.ts` — `detectTemporalAnomalies(submissionId)` runs coverage gap, claim timing cluster, document staleness, and impossible date checks with category TEMPORAL
- Date parsing: all extracted dates are `string | null` — use robust `parseDate()` that handles MM/DD/YYYY, YYYY-MM-DD, MM/DD/YY formats; loss run `policyPeriod` uses "date - date" format
- Financial ratio analysis: `src/services/analysis-financial-ratios.ts` — `analyzeFinancialRatios(submissionId)` runs DSO, A/R ratio, profit margin, and contents-to-building ratio checks with category RATIO
- Contents-to-building ratio ranges by occupancy: retail 20-40%, manufacturing 30-60%, office 10-30%, warehouse 15-50%, restaurant 20-45%; use `classifyOccupancy()` to map freeform occupancy strings
- Risk scoring: `src/services/scoring-engine.ts` — `calculateRiskScore(submissionId)` aggregates non-overridden FraudIndicators, returns ScoreBreakdown with totalScore, severity, and per-category points
- Routing engine: `src/services/routing-engine.ts` — `routeSubmission(submissionId)` uses ThresholdConfig (per-LOB → global → defaults) to auto-approve, escalate, refer to SIU, or assign standard review; creates AuditLog for each decision
- Dynamic API routes: use `[id]` folder naming in `src/app/api/`, params accessed via 2nd arg in withTenant/withRole handlers (e.g., `params?.id`)
- Notification service: `sendNotification(type, recipientEmail, tenantId, data)` — 4 args, tenantId is required 3rd argument
- Prisma enum in withTenantFilter: cast string literals to enum type (e.g., `"SIU_INVESTIGATOR" as Role`) when building where clauses via withTenantFilter
- Audit logging: use `logAudit()` from `src/services/audit-log.ts` for all user-initiated actions; IP address from `x-forwarded-for` or `x-real-ip` headers
- Config API routes: `src/app/api/config/thresholds/` — ADMIN-only, use `withRole(["ADMIN"])`, all mutations audit-logged with THRESHOLD_CHANGED action
- Webhooks: `dispatchWebhookEvent(tenantId, event, data)` from `src/services/webhooks.ts` to fire events to tenant subscribers; HMAC-SHA256 signature in `X-Webhook-Signature` header
- Portal layout: `src/app/portal/layout.tsx` is a server component that loads WhiteLabelConfig; wraps children in `PortalShell` (client) which provides `SessionProvider` and CSS variables for theming
- Portal white-label CSS vars: `--portal-primary`, `--portal-secondary`, `--portal-accent` set on PortalShell, consumed by child components via `var()` references and Tailwind arbitrary values like `text-[var(--portal-primary)]`
- Portal tenant resolution: uses `DEFAULT_TENANT_SLUG` env var; production should parse subdomain from host header
- Dashboard layout: `src/app/dashboard/layout.tsx` is a server component that fetches tenant name; wraps children in `DashboardShell` (client) which provides `SessionProvider` and `ThemeContext`
- Dashboard theme: use `useSyncExternalStore` for theme state (NOT `useState` + `useEffect` with `setState` — triggers `react-hooks/set-state-in-effect` lint error). Persisted in `localStorage` key `dashboard-theme`
- Dashboard dark mode: add `dark:` variant classes to all dashboard components. Toggle via `document.documentElement.classList.toggle("dark")`
- Dashboard role-based nav: `navItems` array with optional `roles` field; SIU Cases = SIU_INVESTIGATOR+ADMIN, Configuration = ADMIN, Audit Trail = ADMIN+COMPLIANCE_OFFICER
- @dnd-kit: use `useDroppable` for columns + `useDraggable` for cards; `DragOverlay` for drag preview; `PointerSensor` with distance constraint to separate clicks from drags
- Recharts: use `ResponsiveContainer` wrapping `BarChart layout="vertical"` for horizontal bar charts; Tooltip formatter type is `(value: number | undefined) => [string, string]`
- Dashboard detail pages: `mx-auto max-w-5xl space-y-6 p-4 sm:p-6` layout with rounded-xl bordered sections
- False positive API: PATCH `/api/submissions/:id/indicators/:indicatorId` with `{ justification }` body — toggles isOverridden on the FraudIndicator record
- Score override API: PATCH `/api/submissions/:id/override-score` with `{ score, justification }` body — SENIOR_UNDERWRITER/ADMIN only, directly sets riskScore and recalculates severity
- Risk report API returns ALL indicators (including overridden) with `isOverridden`, `overriddenBy`, `overrideJustification` fields; score breakdown uses only active indicators
- Action bar pattern: sticky bottom-0 with `bg-white/95 backdrop-blur-sm` for semi-transparent footer with action buttons
- User management API: `/api/config/users` (GET list + POST invite) and `/api/config/users/:id` (PATCH edit role/status), ADMIN-only via `withRole(["ADMIN"])`
- PDF generation: use `pdfkit` for server-side PDF creation; wrap in Promise to collect stream chunks into Buffer; use `new Uint8Array(buffer)` for NextResponse body
- SIU reports API: POST `/api/siu-cases/:id/reports` with `{ reportType }` body generates PDF, stores as evidence attachment, returns PDF download
- State fraud warnings: `src/services/state-fraud-warnings.ts` with `getStateFraudWarning(stateCode)` for all 50 states + DC
- Analytics tabs: Overview/Broker Quality/Compliance at `/dashboard/analytics` — tab-specific data fetching only loads data for active tab
- CSV export: `downloadCSV(filename, headers, rows)` helper creates client-side Blob download — used on all analytics tables
- Broker analytics API: GET `/api/analytics/brokers` with optional `brokerId` param for drill-down detail
- API key management: `/api/config/api-keys` (GET list, POST create) and `/api/config/api-keys/:id` (PATCH revoke), ADMIN-only; keys use `qsk_` prefix + 32 random bytes, stored as SHA-256 hash
- OpenAPI spec: served at `/api/docs/openapi.json` as static JSON from route handler; covers all public API endpoints
- swagger-ui-react: use `dynamic(() => import("swagger-ui-react"), { ssr: false })` for Next.js SSR compatibility
- FraudIndicator field is `recommendedAction` (camelCase), NOT `recommended_action` — Prisma auto-maps snake_case DB columns to camelCase
- Entity verification: `src/services/verification-sos.ts` — `verifyEntity(submissionId)` with mock SOS API, retry logic, and 4 checks (not found, inactive, recent incorporation, virtual office address) with category ENTITY_INTEL
---

## 2026-02-15 - US-009
- What was implemented: Document classification service that auto-classifies uploaded documents by type
- Files changed:
  - src/services/classification.ts (new: classifyDocument function with filename, PDF text, spreadsheet, and image classification strategies)
  - src/lib/s3.ts (added getFromS3 function using GetObjectCommand for downloading files)
  - package.json (added pdf-parse v2 dependency for PDF text extraction)
  - prd.json (marked US-009 as passes: true)
- **Learnings for future iterations:**
  - pdf-parse v2 has a completely different API from v1: use `new PDFParse({ data: Uint8Array })` class, call `.getText()` for text extraction, and `.destroy()` to clean up
  - @types/pdf-parse is for v1 only — do NOT install it with pdf-parse v2 (v2 ships its own types)
  - Classification uses a multi-strategy approach: filename patterns (0.7 confidence), PDF text keywords (up to 0.99), spreadsheet patterns (0.6), image default (0.5)
  - Document status transitions: UPLOADED → CLASSIFYING → EXTRACTING (success) or ERROR (failure)
  - S3 GetObject returns a stream body; use `transformToWebStream().getReader()` to convert to Buffer
---

## 2026-02-15 - US-001
- What was implemented: Project scaffolding with Next.js 16, TypeScript (strict), Tailwind CSS v4 (slate/blue palette), Prisma 7 with PostgreSQL adapter, ESLint + Prettier, env template
- Files changed:
  - package.json (project config, dependencies, scripts)
  - tsconfig.json (TypeScript strict mode)
  - src/app/layout.tsx (root layout with Quantana Shield branding)
  - src/app/page.tsx (minimal landing page)
  - src/app/globals.css (Tailwind v4 theme with blue/slate color palette)
  - src/lib/prisma.ts (Prisma client singleton with PrismaPg adapter)
  - src/components/.gitkeep, src/services/.gitkeep (directory structure)
  - prisma/schema.prisma (base Prisma schema with PostgreSQL)
  - prisma.config.ts (Prisma 7 config)
  - eslint.config.mjs (ESLint flat config with prettier)
  - .prettierrc.json (Prettier config)
  - .env.example (environment variable template)
  - .gitignore (updated for Next.js, Prisma, env files)
  - postcss.config.mjs, next.config.ts (Next.js/PostCSS config)
- **Learnings for future iterations:**
  - Prisma 7 uses `prisma-client` generator (not `prisma-client-js`) and requires either `adapter` or `accelerateUrl` in constructor
  - Tailwind v4 uses `@theme inline` blocks in CSS instead of tailwind.config.js
  - Next.js 16 uses eslint.config.mjs (ESLint flat config) instead of .eslintrc.json
  - The prisma generate command needs DATABASE_URL set even for client generation: `DATABASE_URL="..." npx prisma generate`
---

## 2026-02-15 - US-002
- What was implemented: Database schema for tenants, users, and roles with multi-tenancy support
- Files changed:
  - prisma/schema.prisma (added Role enum, Tenant model, User model with tenant FK)
  - prisma/migrations/20260215000000_add_tenants_and_users/migration.sql (SQL migration)
  - prisma/migrations/migration_lock.toml (migration lock file)
  - prisma/seed.ts (seed script creating test tenant + one user per role)
  - prisma.config.ts (added seed command in migrations config)
  - prd.json (marked US-002 as passes: true)
- **Learnings for future iterations:**
  - Prisma 7 seed config goes inside `migrations: { seed: "npx tsx prisma/seed.ts" }`, NOT at top level
  - `prisma migrate dev` requires a running DB; use `prisma migrate diff --from-empty --to-schema` to generate SQL without one
  - Use `@@unique([tenantId, email])` for compound unique constraints in multi-tenant schemas
  - Use `@@map("tablename")` on models to control the actual PostgreSQL table names
  - Seed script uses upsert for idempotency (safe to run multiple times)
---

## 2026-02-15 - US-003
- What was implemented: Database schema for submissions and documents with full enum types and relations
- Files changed:
  - prisma/schema.prisma (added SubmissionStatus, Severity, Channel, DocumentType, DocumentStatus enums; added Submission and Document models with relations and indexes; added relation fields to Tenant and User)
  - prisma/migrations/20260215000001_add_submissions_and_documents/migration.sql (incremental SQL migration)
  - prd.json (marked US-003 as passes: true)
- **Learnings for future iterations:**
  - Prisma 7 `migrate diff --from-migrations` requires a shadow database; use `--from-empty --to-schema` and manually extract incremental SQL for offline migration generation
  - Named relations (e.g., `@relation("Submitter")`) are required when a model has multiple relations to the same target model
  - Use `@@index` for columns frequently used in WHERE/ORDER BY clauses (tenantId, status, createdAt, submissionId)
  - DocumentType enum has 17 values covering all insurance document types including UNKNOWN as default
---

## 2026-02-15 - US-004
- What was implemented: Database schema for fraud indicators, audit logs, and threshold configs
- Files changed:
  - prisma/schema.prisma (added FraudIndicatorCategory enum, FraudIndicator model, AuditLog model, ThresholdConfig model; added reverse relation fields to Tenant, User, Submission, Document)
  - prisma/migrations/20260215000002_add_fraud_indicators_audit_logs_thresholds/migration.sql (incremental SQL migration)
  - prisma/seed.ts (added default ThresholdConfig upsert for test tenant)
  - prd.json (marked US-004 as passes: true)
- **Learnings for future iterations:**
  - When a model has multiple relations to User (e.g., FraudIndicator.overriddenBy, AuditLog.user), use named relations like `@relation("IndicatorOverrides")` and `@relation("AuditLogUser")`
  - ThresholdConfig uses `lineOfBusiness: null` for the global config; per-LOB overrides have a specific value
  - Seed script uses `findFirst` + conditional `create` for ThresholdConfig since there's no natural unique constraint for upsert (tenantId + null lineOfBusiness)
---

## 2026-02-15 - US-005
- What was implemented: Database schema for SIU cases and white-label configuration
- Files changed:
  - prisma/schema.prisma (added SIUCaseStatus enum, SIUCase model with tenant/submission/user relations, WhiteLabelConfig model with unique tenantId constraint; added reverse relations on Tenant, User, Submission)
  - prisma/migrations/20260215000003_add_siu_cases_and_white_label_configs/migration.sql (incremental SQL migration)
  - prd.json (marked US-005 as passes: true)
- **Learnings for future iterations:**
  - WhiteLabelConfig uses `@unique` on tenantId for 1:1 relationship with Tenant (use `WhiteLabelConfig?` on the Tenant side)
  - SIUCase.notes and SIUCase.evidence use `Json @default("[]")` for arrays of structured objects (notes: {userId, text, timestamp}, evidence: {type, url, description, timestamp})
  - Named relation `@relation("SIUCaseAssignee")` needed to distinguish from other User relations
---

## 2026-02-15 - US-006
- What was implemented: Auth service with JWT login, API key authentication, and middleware
- Files changed:
  - prisma/schema.prisma (added ApiKey model with tenant relation, hashed key storage)
  - prisma/migrations/20260215000004_add_api_keys/migration.sql (incremental SQL migration for api_keys table)
  - src/lib/auth.ts (NextAuth.js config with Credentials provider, JWT strategy, tenantId+role in token)
  - src/app/api/auth/[...nextauth]/route.ts (NextAuth route handler for App Router)
  - src/lib/auth-middleware.ts (extractAuth function for JWT or X-API-Key header, hashApiKey utility)
  - src/app/api/auth/login/route.ts (POST /api/auth/login returning JWT token)
  - src/lib/api-handler.ts (withAuth wrapper that checks auth and injects tenantId, helper functions)
  - .env.example (added EMAIL_PROVIDER placeholder)
  - package.json (added next-auth, bcryptjs, jsonwebtoken + type definitions)
  - prd.json (marked US-006 as passes: true)
- **Learnings for future iterations:**
  - NextAuth v4 with App Router uses route handler in `src/app/api/auth/[...nextauth]/route.ts`
  - NextAuth JWT callback receives `user` only on sign-in; subsequent calls only get `token`
  - API keys stored as SHA-256 hashes in DB; raw key shown once on creation
  - `withAuth` wrapper pattern: extracts auth, gets tenantId, passes `ProtectedRequest` to handler
  - For Next.js App Router route params, use `context.params` as a Promise (Next.js 16 pattern)
  - `getToken` from `next-auth/jwt` works in API routes to extract JWT without session
---

## 2026-02-15 - US-007
- What was implemented: Multi-tenant middleware and role-based access control
- Files changed:
  - src/lib/rbac.ts (new: withTenant, withRole wrappers, withTenantFilter helper, hasRole/hasAnyRole checks, broker path restrictions)
  - prd.json (marked US-007 as passes: true)
- **Learnings for future iterations:**
  - `withTenant()` wraps route handlers with auth + automatic broker path enforcement
  - `withRole(["ADMIN", "UNDERWRITER"], handler)` combines auth + tenant scoping + role checking in one wrapper
  - `withTenantFilter(tenantId, whereClause)` is a simple helper to inject tenantId into any Prisma where clause - use it in all queries
  - Role hierarchy is only for the underwriting line (ADMIN > SENIOR_UNDERWRITER > UNDERWRITER). SIU_INVESTIGATOR and COMPLIANCE_OFFICER are independent - they only match themselves or ADMIN
  - BROKER is auto-blocked from non-portal paths (regex patterns in BROKER_ALLOWED_PATTERNS) within the withTenant middleware
  - API key auth bypasses role checks in withRole (API key permissions are handled separately)
---

## 2026-02-15 - US-008
- What was implemented: Document upload API endpoint with S3 storage and submission creation
- Files changed:
  - package.json (added @aws-sdk/client-s3 dependency)
  - src/lib/s3.ts (new: S3 client utility with uploadToS3 function, path format: {tenantId}/{submissionId}/{documentId}/{filename})
  - src/app/api/submissions/route.ts (new: POST /api/submissions for multipart file upload + submission creation, GET /api/submissions for paginated listing)
  - prd.json (marked US-008 as passes: true)
- **Learnings for future iterations:**
  - Next.js App Router supports `req.formData()` natively for multipart form data - no extra middleware needed
  - File objects from formData have `.arrayBuffer()` method to get buffer for S3 upload
  - Use `withTenant` wrapper from rbac.ts for routes accessible by all authenticated roles (including BROKER)
  - Broker submissions endpoint is already in BROKER_ALLOWED_PATTERNS in rbac.ts
  - Document records are created before S3 upload so we have the document ID for the S3 key path; s3Key is updated after successful upload
  - File type validation uses both MIME type and file extension as fallback (browsers may send generic MIME types)
---

## 2026-02-15 - US-010
- What was implemented: ACORD 125 data extraction service with regex/pattern-based field extraction from PDF text
- Files changed:
  - src/services/extraction-acord125.ts (new: extractAcord125 function with 16 field extractors, per-field confidence metadata, PDF text parsing via pdf-parse v2)
  - prd.json (marked US-010 as passes: true)
- **Learnings for future iterations:**
  - Extraction service pattern: `findUniqueOrThrow` to get doc → `extractPdfText(s3Key)` → run field extractors → build result with `_metadata` for per-field confidence → `prisma.document.update` with `extractedData` + `status: "ANALYZED"` → catch errors and set `status: "ERROR"`
  - Use `JSON.parse(JSON.stringify(data))` when writing complex typed objects to Prisma `Json` columns — direct casting to `Record<string, unknown>` doesn't satisfy `InputJsonValue`
  - `extractPdfText(s3Key)` helper is reusable across all extraction services (same pdf-parse v2 pattern from classification)
  - Each field extractor returns `{ value, confidence: { confidence, source } }` or null — this feeds both the extracted value and the `_metadata` confidence map
  - `extractField()` helper tries multiple regex patterns in order and returns first match — good for ACORD forms where labels vary between versions
---

## 2026-02-15 - US-011
- What was implemented: ACORD 130 (Workers' Comp) and ACORD 140 (Property) data extraction services
- Files changed:
  - src/services/extraction-acord130.ts (new: extractAcord130 function with payroll classification table parsing, e-mod rate, prior carrier extraction)
  - src/services/extraction-acord140.ts (new: extractAcord140 function with multi-location property parsing, building/contents/BI values, construction details, TIV and deductible)
  - prd.json (marked US-011 as passes: true)
- **Learnings for future iterations:**
  - ACORD 130 payroll classification tables vary widely — use two-pass approach: first try full pattern (code + description + employee count + payroll), then fallback to simpler pattern (code + description + optional payroll)
  - ACORD 140 property locations are often numbered (LOC #1, LOCATION 2) — split text on location headers to parse each location block independently
  - For extraction services with nested array fields (like payrollByClassification, propertyLocations), return `[]` default instead of `null` for easier downstream consumption
  - Each extraction service follows same pattern but with its own local `extractField`, `parseCurrency`, `parseInteger`, `extractPdfText` helpers — these could be refactored to a shared module if more extractors are needed
  - Construction type extraction uses both regex patterns and keyword matching as fallback for common types (FRAME, JOISTED MASONRY, FIRE RESISTIVE, etc.)
---

## 2026-02-15 - US-012
- What was implemented: Loss run and financial statement data extraction services
- Files changed:
  - src/services/extraction-loss-run.ts (new: extractLossRun function with carrier name, policy period, individual claims parsing via 3 strategies, totalClaimCount, totalIncurred with normalization)
  - src/services/extraction-financial-statement.ts (new: extractFinancialStatement function with income statement fields (revenue, COGS, gross profit, operating expenses, net income) and balance sheet fields (total assets, total liabilities, A/R, A/P))
  - prd.json (marked US-012 as passes: true)
- **Learnings for future iterations:**
  - Loss run formats vary significantly across carriers — use 3-tier claim parsing: (1) full table pattern with all columns, (2) simplified pattern with claim # + date + amount, (3) individual claim block parsing
  - Financial statements use parentheses for negative numbers — `parseCurrency` must handle `(1,234.56)` → `-1234.56`
  - Loss run normalization maps abbreviations to standard statuses (e.g., "CLSD" → "CLOSED", "OPN" → "OPEN") and computes totalIncurred from paid + reserve when missing
  - totalClaimCount and totalIncurred have fallback calculations from parsed claims when explicit totals aren't found in the document (with lower confidence score of 0.5)
  - Each extraction service continues to use its own local helpers following the established pattern
---

## 2026-02-15 - US-013
- What was implemented: COI and entity document extraction services
- Files changed:
  - src/services/extraction-coi.ts (new: extractCOI function with insured/insurer name, policy details, coverage types, liability limits, agent contact extraction)
  - src/services/extraction-entity-doc.ts (new: extractEntityDocument function with entity name/type, state of incorporation, registered agent, officers array, EIN extraction)
  - prd.json (marked US-013 as passes: true)
- **Learnings for future iterations:**
  - COI documents use ACORD 25/28 format with standardized sections: insurer lettering (A-F), coverage types grid, limits columns — regex patterns target these specific layouts
  - Entity type detection uses two-pass approach: (1) explicit field extraction (e.g., "Entity Type: LLC"), (2) keyword scanning in full text as fallback with lower confidence
  - Officer extraction uses dual patterns: title-first ("President: John Smith") and name-first ("John Smith, President") with deduplication by name
  - EIN format is always XX-XXXXXXX (2 digits, dash, 7 digits) — regex captures both with and without dash
  - State of incorporation extraction uses a comprehensive state name + abbreviation pattern to match after common prefixes like "State of Incorporation:", "Organized in", "Laws of"
  - Coverage types in COIs include special characters (e.g., "E&O", "PRODUCTS/COMPLETED OPERATIONS") — regex must escape & and / in pattern construction
---

## 2026-02-15 - US-014
- What was implemented: Cross-document validation service for revenue and payroll checks
- Files changed:
  - src/services/validation-cross-doc.ts (new: validateRevenue and validatePayroll functions with FraudIndicator creation)
  - prd.json (marked US-014 as passes: true)
- **Learnings for future iterations:**
  - Cross-doc validation pattern: query ANALYZED documents by type for the submission, extract the typed data from `extractedData` JSON, compare values, and create FraudIndicator records with category CROSS_DOC
  - Prisma JSON nullable fields: don't use `{ not: null }` in where clauses — it requires `Prisma.JsonNullValueFilter`. Instead, filter without the null check and do a runtime `!doc.extractedData` check after the query
  - `findExtractedData<T>(submissionId, documentType)` is a reusable helper for all cross-doc validators — returns typed extracted data + documentId
  - Revenue variance: calculated as percentage of the larger value to handle both over- and under-reporting (>15% = HIGH, >25% = CRITICAL)
  - Payroll variance: uses dual thresholds — absolute ($2,000) AND percentage (10%) — flag only if absolute exceeds $2,000, then severity depends on percentage
  - Both functions skip gracefully (early return, no error) if required documents are missing — important for partial submissions
  - Evidence JSON includes both source values and document IDs for traceability in the UI
---

## 2026-02-15 - US-015
- What was implemented: Cross-document validation for employee count, loss history, and property values
- Files changed:
  - src/services/validation-cross-doc.ts (added validateEmployeeCount, validateLossHistory, validatePropertyValues functions; added imports for Acord140ExtractedData and LossRunExtractedData; added employeeCount to PayrollTaxExtractedData)
  - prd.json (marked US-015 as passes: true)
- **Learnings for future iterations:**
  - Employee count validation uses a cascading source strategy: prefer ACORD 125 `numberOfEmployees`, fallback to summing ACORD 130 `payrollByClassification[].employeeCount`
  - Loss history validation aggregates claims from ALL loss run documents (multiple loss runs per submission) — use `findMany` not `findFirst`
  - Loss history omission (lossDisclosure=false but claims exist) is the strongest signal — flagged as CRITICAL with 0.9 confidence
  - Property value validation uses a configurable assessed-to-insured ratio (default 0.7) as a heuristic since real tax assessor data isn't available in MVP
  - Property validation iterates per-location (each property in the ACORD 140) and creates separate fraud indicators per location
  - All three functions follow the established cross-doc pattern: findExtractedData helper, skip gracefully if docs missing, JSON.parse(JSON.stringify()) for evidence
---

## 2026-02-15 - US-016
- What was implemented: PDF metadata forensics service that analyzes PDF metadata to detect forged or altered documents
- Files changed:
  - src/services/forensics-pdf-metadata.ts (new: analyzePdfMetadata function with 4 forensic checks)
  - prd.json (marked US-016 as passes: true)
- **Learnings for future iterations:**
  - pdf-parse v2 uses `getInfo()` (not `getMetadata()`) for document metadata — returns `InfoResult` with `.info` (raw PDF info dict), `.total` (page count), and `.getDateNode()` for parsed dates
  - `getDateNode()` returns `DateNode` with `CreationDate`, `ModDate` as proper `Date | null` objects — no need for manual PDF date string parsing
  - `getText()` returns a `TextResult` object with `.text` property (string), not a raw string
  - Forensic service pattern: fetch document with `include: { submission: true }`, download from S3, extract metadata, run checks, create FraudIndicator records with category FORENSIC
  - For detecting personal names in PDF metadata, use a two-pass approach: (1) exclude known software patterns, (2) check for 2-4 capitalized words pattern
  - Multiple PDF producers check: compare `producer` vs `creator` fields (stripped of version numbers) — different base names suggest document assembly from multiple sources
  - Image-only PDF detection: use text layer length threshold (>50 chars = has text layer)
---

## 2026-02-15 - US-017
- What was implemented: Statistical anomaly detection service with 4 checks: round numbers, revenue growth, COGS margin, payroll-per-head
- Files changed:
  - src/services/detection-statistical.ts (new: detectStatisticalAnomalies function orchestrating 4 parallel checks)
  - src/services/industry-benchmarks.ts (new: NAICS sector-code-to-benchmark lookup table with COGS margin, payroll-per-head, and revenue growth ranges)
  - prd.json (marked US-017 as passes: true)
- **Learnings for future iterations:**
  - Statistical detection pattern: main function fetches submission, then delegates to parallel check functions (round numbers, revenue growth, COGS margin, payroll-per-head) via Promise.all
  - Industry benchmarks keyed by 2-digit NAICS sector code — use `getBenchmark(naicsCode)` which auto-strips to 2-digit prefix and falls back to DEFAULT_BENCHMARK
  - Round-number detection checks exact $100K multiples across ALL financial fields (ACORD 125 revenue/premium, ACORD 130 payroll, financial statement revenue/COGS/netIncome/assets/liabilities)
  - COGS margin deviation threshold is 15 percentage points from the expected range edges (not relative %)
  - Payroll-per-head uses cascading employee count source: prefer ACORD 125 `numberOfEmployees`, fallback to summing ACORD 130 classification employee counts
  - Revenue growth uses ACORD 125 as "current" revenue and financial statement as "prior year" — this is a heuristic that works for applications submitted with last fiscal year financials
---

## 2026-02-15 - US-018
- What was implemented: Temporal analysis service with 4 anomaly detection checks
- Files changed:
  - src/services/detection-temporal.ts (new: detectTemporalAnomalies function orchestrating 4 parallel checks)
  - prd.json (marked US-018 as passes: true)
- **Learnings for future iterations:**
  - Temporal detection follows same pattern as statistical detection: main function fetches submission for tenantId, delegates to parallel check functions via Promise.all
  - All date fields in extraction types are `string | null` — need a robust `parseDate()` helper that handles MM/DD/YYYY, YYYY-MM-DD, MM-DD-YYYY, and MM/DD/YY formats
  - Loss run `policyPeriod` is a string like "MM/DD/YYYY - MM/DD/YYYY" — split on dash/to separators with regex `\s*[-–—to]+\s*` to handle en-dash and em-dash variants
  - Coverage gap detection: compare most recent prior coverage end date (from COIs and loss runs) against ACORD 125 requested effective date — only flag if requested date is AFTER prior coverage ended
  - Claim timing clusters: require at least 3 claims to detect meaningful patterns; check each claim against both policy inception (60 days after start) and expiration (60 days before end)
  - Document staleness uses `doc.createdAt` as proxy for document date — real implementation would extract date from document content
  - Impossible dates check: loss run effective date AFTER policy effective is flagged because loss runs should cover prior periods; COI expiration before effective is logically impossible
---

## 2026-02-15 - US-019
- What was implemented: Financial ratio analysis service with 4 checks: DSO anomaly, A/R ratio, profit margin, contents-to-building ratio
- Files changed:
  - src/services/analysis-financial-ratios.ts (new: analyzeFinancialRatios function orchestrating 4 parallel checks)
  - prd.json (marked US-019 as passes: true)
- **Learnings for future iterations:**
  - Financial ratio analysis follows the same detection service pattern: main function fetches submission for tenantId, delegates to parallel check functions via Promise.all, each creates FraudIndicator with category RATIO
  - DSO check uses prior-year A/R from financial statement and compares against 45-day industry norm baseline — if DSO exceeds norm by >20% AND revenue is growing, it's a classic fictitious revenue indicator
  - Contents-to-building ratio varies by occupancy classification — use `classifyOccupancy()` to map freeform occupancy strings to categories (retail, manufacturing, office, warehouse, restaurant)
  - Industry profit margin benchmarks stored inline by NAICS sector code; threshold is industry average * 1.5 (50% above average)
  - A/R ratio check is straightforward: flag if accounts receivable > 40% of revenue from financial statement
---

## 2026-02-15 - US-020
- What was implemented: Risk scoring engine that aggregates all FraudIndicator records into a composite risk score (0-100) with severity classification
- Files changed:
  - src/services/scoring-engine.ts (new: calculateRiskScore function with weighted scoring, severity classification, and category breakdown)
  - prd.json (marked US-020 as passes: true)
- **Learnings for future iterations:**
  - Risk scoring uses weighted severity: CRITICAL=25, HIGH=15, MEDIUM=5, LOW=1, capped at 100
  - Severity classification: >85 or any CRITICAL → CRITICAL, 60-85 → HIGH, 35-60 → MEDIUM, <35 → LOW, 0 indicators → CLEAN
  - Only non-overridden indicators (isOverridden: false) are included in the score — overridden false-positives don't count
  - Score breakdown tracks both counts and points per FraudIndicatorCategory — useful for the UI bar chart in US-035
  - Function returns a ScoreBreakdown object with totalScore, severity, categoryCounts, categoryPoints, and indicatorCounts for downstream use
---

## 2026-02-15 - US-021
- What was implemented: Auto-routing service that routes submissions based on risk score and tenant threshold configuration
- Files changed:
  - src/services/routing-engine.ts (new: routeSubmission function with 4 routing paths: auto-approve, escalate to senior UW, refer to SIU, standard UW review)
  - prd.json (marked US-021 as passes: true)
- **Learnings for future iterations:**
  - Routing priority order matters: SIU referral (forged docs) > auto-approve (low risk) > escalate (high risk) > default review
  - ThresholdConfig lookup uses cascading: per-LOB override → global (lineOfBusiness=null) → hardcoded defaults
  - SIU referral checks for FORENSIC category + CRITICAL severity as the "forged/fabricated document" signal
  - Auto-approve requires BOTH score below threshold AND no MEDIUM+ indicators — not just score alone
  - Escalation assigns to SENIOR_UNDERWRITER; default review assigns to any UNDERWRITER or SENIOR_UNDERWRITER
  - SIU referral auto-creates an SIUCase record with OPEN status assigned to an SIU_INVESTIGATOR
  - All routing decisions create AuditLog entries with action, reason, and assigned user info
---

## 2026-02-15 - US-022
- What was implemented: Submission processing pipeline orchestrator that runs all processing steps in sequence when a submission is uploaded
- Files changed:
  - src/services/pipeline.ts (new: processSubmission function orchestrating classify → extract → detect → score → route pipeline)
  - prd.json (marked US-022 as passes: true)
- **Learnings for future iterations:**
  - Pipeline uses `Promise.allSettled` for each step to handle errors gracefully — if one document fails classification/extraction, the remaining documents still process
  - Document type to extractor mapping: only 7 of 17 document types have extractors (ACORD 125/130/140, LOSS_RUN, FINANCIAL_STATEMENT, COI, ENTITY_DOC); other types silently skip extraction
  - PDF forensics runs on documents with `fileType === "application/pdf"` — filters by MIME type, not document type enum
  - Detection engines (cross-doc, statistical, temporal, ratio) run in parallel since they're independent and all take submissionId
  - Scoring and routing run sequentially after all detection engines complete (they depend on accumulated FraudIndicator records)
  - Pipeline creates AuditLog entries for PROCESSING_STARTED and PROCESSING_COMPLETED with timing and error counts
  - Error counts from each step tracked via `countErrors()` helper and included in completion audit log
---

## 2026-02-15 - US-023
- What was implemented: Audit logging service with logAudit function and GET /api/audit-logs endpoint
- Files changed:
  - src/services/audit-log.ts (new: logAudit function with typed AuditAction union, LogAuditParams interface)
  - src/app/api/audit-logs/route.ts (new: GET /api/audit-logs endpoint with pagination, filtering by action/submissionId/userId/dateRange, ADMIN and COMPLIANCE_OFFICER role restriction)
  - prd.json (marked US-023 as passes: true)
- **Learnings for future iterations:**
  - Audit log service is a thin wrapper around prisma.auditLog.create — keeps it simple and focused
  - AuditAction type union enforces consistent action names across all callers
  - GET endpoint uses withRole(["ADMIN", "COMPLIANCE_OFFICER"]) for access control — only these roles should see audit trails
  - Default page size is 50 (vs 20 for submissions) since audit logs are scanned more than paginated
  - No update/delete endpoints — audit logs are append-only by design for compliance
  - Existing audit log creation in routing-engine.ts and pipeline.ts directly uses prisma.auditLog.create — future refactoring could migrate those to use logAudit() for consistency
---

## 2026-02-15 - US-024
- What was implemented: Notification service with configurable email provider (SendGrid/SMTP), 7 email templates using tenant white-label branding
- Files changed:
  - src/services/notification.ts (new: sendNotification function with 7 template builders, dual email provider support, white-label branding integration)
  - .env.example (added SMTP_HOST, SMTP_PORT, SMTP_SECURE, SMTP_USER, SMTP_PASSWORD, SENDGRID_API_KEY, EMAIL_FROM_ADDRESS)
  - package.json (added nodemailer, @sendgrid/mail, @types/nodemailer)
  - prd.json (marked US-024 as passes: true)
- **Learnings for future iterations:**
  - Notification service follows a template builder pattern: each NotificationType maps to a build function that returns {subject, html}
  - Broker-facing templates (SUBMISSION_RECEIVED, APPROVED, DECLINED, INFO_NEEDED, ESCALATION_NOTICE) never reveal fraud scores, risk details, or detection logic
  - Internal templates (NEW_SUBMISSION_FOR_REVIEW, SIU_REFERRAL) include severity, indicator count, and risk details for underwriter/SIU context
  - All emails use a shared wrapInLayout() function that applies tenant branding (logo, colors, footer, support info, legal links)
  - From address priority: WhiteLabelConfig.supportEmail → custom email domain → default quantanashield.com
  - sendNotification() catches all errors to prevent email failures from breaking main workflows (logging only)
  - EMAIL_PROVIDER env var selects between "sendgrid" (using @sendgrid/mail) and "smtp" (using nodemailer)
  - nodemailer transport is created per-send (not cached) since the service is stateless; for high volume, consider a persistent transport
---

## 2026-02-15 - US-025
- What was implemented: Email intake service that parses inbound emails and creates submissions from their attachments
- Files changed:
  - src/services/email-intake.ts (new: processInboundEmail function with tenant lookup, zip extraction, file upload, and auto-acknowledgment)
  - src/app/api/intake/email/route.ts (new: POST /api/intake/email webhook endpoint supporting both multipart form-data and JSON payloads)
  - package.json (added jszip dependency for zip file extraction)
  - prd.json (marked US-025 as passes: true)
- **Learnings for future iterations:**
  - Email intake webhook is unauthenticated (called by email providers like SendGrid Inbound Parse) — security comes from validating recipient email format and tenant lookup
  - Tenant slug extracted from recipient email: `submissions@{tenant-slug}.quantanashield.com` using regex
  - Sender lookup uses `tenantId_email` compound unique constraint to find the user in the correct tenant
  - Zip file extraction uses JSZip's `loadAsync()` — handles nested zip structures, filters out system files (__MACOSX, dotfiles), only extracts accepted file types
  - SendGrid Inbound Parse can send attachments either as direct form-data file entries OR via `attachment-info` JSON + named file entries — handle both patterns
  - From address parsing handles both raw email (`user@example.com`) and formatted (`John Smith <user@example.com>`) via angle bracket extraction
  - Insured name derived from email subject line with `fwd:` and `re:` prefixes stripped
  - Error notification sent when no valid attachments found; success acknowledgment includes tracking URL with portal link
---

## 2026-02-15 - US-026
- What was implemented: Core API endpoints for submissions CRUD and status management
- Files changed:
  - src/app/api/submissions/[id]/route.ts (new: GET /api/submissions/:id with full detail, documents, submitter, assigned underwriter, fraud indicator count)
  - src/app/api/submissions/[id]/status/route.ts (new: PATCH /api/submissions/:id/status with approve/decline/request-info/refer-to-siu actions, justification required for decline, audit logging, notifications, SIU case creation)
  - src/app/api/submissions/[id]/indicators/route.ts (new: GET /api/submissions/:id/indicators with category/severity filtering, ordered by severity)
  - src/app/api/submissions/[id]/risk-report/route.ts (new: GET /api/submissions/:id/risk-report with score breakdown by category, indicator counts, explainability text, prior submissions from same entity)
  - src/app/api/submissions/route.ts (updated: GET /api/submissions with dateRange and broker filters, added assignedUnderwriter include)
  - src/lib/rbac.ts (updated: BROKER_ALLOWED_PATTERNS — confirmed existing patterns sufficient for broker access)
  - prd.json (marked US-026 as passes: true)
- **Learnings for future iterations:**
  - Next.js App Router dynamic route params: use `[id]` folder naming, params available as 2nd arg in `withTenant`/`withRole` handler callbacks
  - Broker access control: withTenant middleware checks BROKER_ALLOWED_PATTERNS first, then individual endpoints enforce ownership (submitterId === userId)
  - sendNotification requires 4 args: `(type, recipientEmail, tenantId, data)` — don't forget tenantId as 3rd arg
  - Prisma enum typing: use `"SIU_INVESTIGATOR" as Role` when passing string literals to Prisma where clauses that expect enum types via `withTenantFilter`
  - Risk report explainability: generate plain-English summaries from indicator severity/category aggregations for underwriter consumption
  - Prior submissions: query by insuredName for same-entity history linking, exclude current submission with `{ not: id }`
  - Severity ordering: use `{ severity: "asc" }` in Prisma orderBy to get CRITICAL first (enum order: CRITICAL, HIGH, MEDIUM, LOW, CLEAN)
---

## 2026-02-15 - US-027
- What was implemented: Threshold management API endpoints for viewing and updating fraud detection thresholds
- Files changed:
  - src/app/api/config/thresholds/route.ts (new: GET for current config with global + per-LOB overrides, PUT for updating global thresholds with validation and audit logging)
  - src/app/api/config/thresholds/[lineOfBusiness]/route.ts (new: PUT for setting per-LOB override, DELETE for removing per-LOB override with fallback to global)
  - src/app/api/config/thresholds/preview/route.ts (new: GET preview endpoint that simulates re-routing of last 30 days submissions with proposed thresholds)
  - prd.json (marked US-027 as passes: true)
- **Learnings for future iterations:**
  - Config API routes use `withRole(["ADMIN"])` since only admins can modify thresholds
  - ThresholdConfig lookup: `lineOfBusiness: null` = global, non-null = per-LOB override
  - Cross-validation (approveBelow < escalateAbove) must use existing values as defaults when partial updates are sent
  - Preview endpoint batch-fetches all indicators for efficiency rather than per-submission queries
  - Preview re-routing follows same logic as routing-engine.ts: SIU referral > auto-approve > escalate > default review
  - Dynamic route params in `[lineOfBusiness]` need `decodeURIComponent()` for LOBs with spaces/special chars
  - All threshold changes create THRESHOLD_CHANGED audit log entries with before/after values
---

## 2026-02-15 - US-028
- What was implemented: Webhook subscription and delivery system with HMAC-SHA256 signing
- Files changed:
  - prisma/schema.prisma (added WebhookSubscription model with tenant relation)
  - prisma/migrations/20260215000005_add_webhook_subscriptions/migration.sql (SQL migration for webhook_subscriptions table)
  - src/services/webhooks.ts (new: webhook delivery service with HMAC signing, retry logic, event dispatch)
  - src/app/api/webhooks/route.ts (new: POST /api/webhooks for creating subscriptions, GET /api/webhooks for listing active subscriptions)
  - src/app/api/webhooks/[id]/route.ts (new: DELETE /api/webhooks/:id for deactivating subscriptions)
  - prd.json (marked US-028 as passes: true)
- **Learnings for future iterations:**
  - Webhook secrets use `whsec_` prefix + 32 random bytes hex for easy identification
  - HMAC-SHA256 signature sent in `X-Webhook-Signature` header; computed over the stringified JSON body
  - Retry logic: 3 attempts with exponential backoff (1s, 2s, 4s); 4xx errors are non-retryable, 5xx errors retry
  - Webhook delivery uses `Promise.allSettled` to fire all matching subscriptions in parallel without blocking the main flow
  - `dispatchWebhookEvent(tenantId, event, data)` is the entry point — finds active subscriptions matching the event type
  - Secret is returned only on POST creation response; GET endpoint omits the secret for security
  - DELETE endpoint soft-deletes by setting `isActive: false` rather than hard deleting the record
  - Event types: submission.processed, submission.approved, submission.declined, submission.escalated, fraud.flag_raised, siu.case_created
---

## 2026-02-15 - US-029
- What was implemented: Submission portal layout with white-label theming and login page
- Files changed:
  - src/app/portal/layout.tsx (new: server-side portal layout that loads WhiteLabelConfig for tenant by slug)
  - src/app/portal/page.tsx (new: portal home redirects to /portal/submissions)
  - src/app/portal/login/page.tsx (new: login page with email/password form, styled with tenant branding via CSS variables)
  - src/app/portal/submissions/page.tsx (new: placeholder submissions list page for navigation)
  - src/components/portal/PortalShell.tsx (new: client component wrapping SessionProvider, header, main content, footer with white-label config)
  - src/components/portal/PortalHeader.tsx (new: responsive header with tenant logo/name, nav links, sign out button, mobile navigation)
  - src/app/api/portal/config/route.ts (new: GET /api/portal/config?slug= endpoint for fetching tenant white-label config)
  - .env.example (added DEFAULT_TENANT_SLUG)
  - prd.json (marked US-029 as passes: true)
- **Learnings for future iterations:**
  - Portal white-label theming uses CSS custom properties (`--portal-primary`, `--portal-secondary`, `--portal-accent`) set on the shell wrapper div, consumed by child components via `var()` references
  - Tenant slug resolved from `DEFAULT_TENANT_SLUG` env var (production would use subdomain/host header parsing)
  - PortalShell is a client component wrapping SessionProvider — portal layout is a server component that fetches config and passes it down
  - No Quantana branding appears anywhere in the portal — only tenant name/logo displayed
  - Login page uses NextAuth's `signIn("credentials", { redirect: false })` for client-side auth flow with error handling
  - Footer dynamically shows tenant branding, legal links, and support contact from WhiteLabelConfig
  - Responsive design: mobile navigation bar collapses below md breakpoint, user name hidden on small screens
  - Next.js Image component required for external logo URLs (lint warning with plain `<img>`)
---

## 2026-02-15 - US-030
- What was implemented: Submission portal document upload page with drag-and-drop file upload, form fields, and file management
- Files changed:
  - src/app/portal/submissions/new/page.tsx (new: upload page with react-dropzone, insured name/LOB/effective date form, file list with status badges, progress indicators, remove buttons)
  - package.json (added react-dropzone dependency)
  - prd.json (marked US-030 as passes: true)
- **Learnings for future iterations:**
  - react-dropzone `accept` prop uses MIME types as keys with extension arrays as values: `{ "application/pdf": [".pdf"], ... }`
  - Portal form pattern: use the same input styling as login page with `focus:border-[var(--portal-primary)]` and `focus:ring-2 focus:ring-[var(--portal-primary)]/20`
  - For select/dropdown inputs, the same input styling pattern applies but with `<select>` element
  - File upload to API: build `FormData`, append form fields and files, POST to `/api/submissions` — no Content-Type header needed (browser sets multipart boundary automatically)
  - Use inline `style={{ backgroundColor: "var(--portal-primary)" }}` for buttons rather than Tailwind arbitrary values, since CSS vars work more reliably with opacity in inline styles
  - File type icons: map extensions to color classes (pdf=red, xlsx=green, docx=blue, images=purple) for visual differentiation in the file list
  - After submission POST, redirect to `/portal/submissions/${submissionId}/review` for the classification review step (US-031)
---

## 2026-02-15 - US-031
- What was implemented: Classification review and validation results page for the broker submission portal
- Files changed:
  - src/app/portal/submissions/[id]/review/page.tsx (new: review page with document classification list, type correction dropdowns, confidence badges, validation results grouped by severity, re-upload button, confirm & submit)
  - src/app/api/submissions/[id]/documents/[docId]/route.ts (new: PATCH for document type correction, PUT for document re-upload/replacement)
  - src/app/api/submissions/[id]/confirm/route.ts (new: POST to confirm submission and trigger processing pipeline)
  - src/lib/rbac.ts (updated: added `/api/submissions/:id/confirm` to BROKER_ALLOWED_PATTERNS)
  - prd.json (marked US-031 as passes: true)
- **Learnings for future iterations:**
  - Validation results are generated client-side from document data — no fraud detection logic shown to brokers, only data quality issues (unknown type, low confidence, processing errors, missing common docs)
  - Document type correction sets classificationConfidence to 1.0 since it's a manual user selection
  - Re-upload (PUT) resets document to UPLOADED status with UNKNOWN type and null confidence, clearing extractedData for re-processing
  - Confirm endpoint fires `processSubmission()` asynchronously (fire-and-forget) to avoid blocking the HTTP response
  - Confirm endpoint blocks if any documents still have UNKNOWN type — enforces classification before processing
  - Hidden file input pattern: store `reuploadingDocId` in state, trigger `fileInputRef.current.click()`, handle file in onChange
  - BROKER_ALLOWED_PATTERNS in rbac.ts must include any new API paths brokers need access to — check and update when adding new broker-facing endpoints
---

## 2026-02-15 - US-032
- What was implemented: Submission portal submission list page and detail/status page for brokers
- Files changed:
  - src/app/portal/submissions/page.tsx (replaced: full submission list with table view, status badges, filters by status/date range/insured name search, pagination, INFO_NEEDED notification badge, mobile card view, empty state)
  - src/app/portal/submissions/[id]/page.tsx (new: submission detail page with status banner, submission info, document list, audit log timeline, info-needed messages from underwriter, auto-refresh while processing)
  - src/components/portal/PortalHeader.tsx (updated: added notification badge on "My Submissions" nav link showing count of INFO_NEEDED submissions, polls every 30 seconds)
  - prd.json (marked US-032 as passes: true)
- **Learnings for future iterations:**
  - Broker portal pages should NOT show fraud detection details (risk scores, severity, indicator counts) — only status, document info, and underwriter messages
  - REFERRED_TO_SIU status is displayed as "Under Review" to brokers to avoid revealing fraud investigation details
  - Audit log endpoint (/api/audit-logs) requires ADMIN or COMPLIANCE_OFFICER role — broker detail page gracefully handles 403 by showing no timeline section
  - Auto-refresh pattern: use `setInterval` inside a `useEffect` that checks `submission?.status === "PROCESSING"` to poll for updates every 5 seconds while processing
  - For TypeScript issues with `Record<string, unknown>` values in JSX expressions, extract rendering into a separate component rather than inline complex type narrowing
  - Notification badge count: use `limit=1` query with pagination total for efficient count-only fetches
  - Portal header polls INFO_NEEDED count every 30 seconds with interval cleanup in useEffect return
---

## 2026-02-15 - US-033
- What was implemented: Underwriter dashboard shell with sidebar navigation, header, dark/light mode toggle, login page, and role-based navigation
- Files changed:
  - src/app/dashboard/layout.tsx (new: server-side dashboard layout fetching tenant name, wrapping in DashboardShell)
  - src/app/dashboard/page.tsx (new: redirects to /dashboard/triage)
  - src/app/dashboard/login/page.tsx (new: login page with Quantana Shield branding, role validation for dashboard-allowed roles)
  - src/app/dashboard/triage/page.tsx (new: placeholder triage board page)
  - src/app/dashboard/analytics/page.tsx (new: placeholder analytics page)
  - src/app/dashboard/siu-cases/page.tsx (new: placeholder SIU cases page)
  - src/app/dashboard/config/page.tsx (new: placeholder configuration page)
  - src/app/dashboard/audit-trail/page.tsx (new: placeholder audit trail page)
  - src/components/dashboard/DashboardShell.tsx (new: client component with SessionProvider, theme context via useSyncExternalStore, sidebar state management)
  - src/components/dashboard/DashboardSidebar.tsx (new: sidebar with role-based nav items, collapsible on desktop, slide-over on mobile, user info at bottom)
  - src/components/dashboard/DashboardHeader.tsx (new: header with mobile menu button, tenant name, "Powered by Quantana Shield", dark/light toggle, user name + role badge, sign out)
  - prd.json (marked US-033 as passes: true)
- **Learnings for future iterations:**
  - Dashboard uses a separate layout from portal — `/dashboard` route group has its own `layout.tsx` with `DashboardShell` component
  - Theme management uses `useSyncExternalStore` instead of `useState` + `useEffect` with `setState` to avoid the `react-hooks/set-state-in-effect` lint error
  - Dark mode classes: use `dark:bg-slate-900`, `dark:text-white`, `dark:border-slate-700` etc. with Tailwind's `dark:` variant; toggle via `document.documentElement.classList.toggle("dark")`
  - Theme persisted in `localStorage` key `dashboard-theme`; falls back to system preference via `prefers-color-scheme` media query
  - Role-based nav visibility: filter `navItems` array by optional `roles` field — items without `roles` are visible to all; SIU Cases requires SIU_INVESTIGATOR or ADMIN; Configuration requires ADMIN; Audit Trail requires ADMIN or COMPLIANCE_OFFICER
  - Dashboard login page checks role after sign-in via `/api/auth/session` endpoint — blocks BROKER role from accessing dashboard
  - Sidebar is collapsible on desktop (icon-only mode with `w-16`) and uses slide-over overlay on mobile
  - Header shows subtle "Powered by Quantana Shield" branding alongside the tenant name
---

## 2026-02-15 - US-034
- What was implemented: Underwriter dashboard triage board with kanban view, drag-and-drop status changes, filters, and bulk approve
- Files changed:
  - src/app/dashboard/triage/page.tsx (replaced placeholder: full kanban board with DndContext, column grouping, filters, bulk approve, decline justification modal)
  - src/components/dashboard/triage/KanbanColumn.tsx (new: droppable kanban column with color-coded headers, count badges, drop zone highlighting)
  - src/components/dashboard/triage/SubmissionCard.tsx (new: draggable submission card with risk score gauge, severity badge, indicator count, checkbox selection)
  - package.json (added @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities)
  - prd.json (marked US-034 as passes: true)
- **Learnings for future iterations:**
  - @dnd-kit uses `useDroppable` + `useDraggable` hooks — droppable zones are identified by `id` matching column IDs, draggable items use submission IDs
  - `DragOverlay` renders a copy of the dragged card for smooth visual feedback; the original card is dimmed with `opacity-40`
  - `PointerSensor` with `activationConstraint: { distance: 8 }` prevents accidental drags from clicks
  - `COLUMNS` defined `as const` creates readonly tuple types — use `(c.statuses as readonly string[]).includes(status)` to avoid TS error when checking plain `string` against literal tuple
  - Kanban column status mapping: NEW = PROCESSING + UNDER_REVIEW, IN_REVIEW = INFO_NEEDED, other columns map 1:1 to submission statuses
  - Drag from one column to another triggers PATCH `/api/submissions/:id/status` with the appropriate action; decline requires justification via modal
  - Indicator counts fetched per-submission via `/api/submissions/:id/indicators` in parallel — could be optimized with a batch endpoint in the future
  - Bulk approve button only enables when LOW/CLEAN risk submissions are selected (per AC requirement)
  - Risk score gauge uses SVG circle with `strokeDasharray`/`strokeDashoffset` for the progress arc
---

## 2026-02-15 - US-035
- What was implemented: Underwriter dashboard submission risk report detail view at /dashboard/submissions/:id
- Files changed:
  - src/app/dashboard/submissions/[id]/page.tsx (new: full risk report page with large gauge chart, category breakdown bar chart via Recharts, filterable/sortable indicator list with expandable evidence panels, explainability section, prior submissions table)
  - package.json (added recharts dependency for chart visualizations)
  - prd.json (marked US-035 as passes: true)
- **Learnings for future iterations:**
  - Recharts `Tooltip` formatter type: use `(value: number | undefined) => [string, string]` to satisfy strict TypeScript — the value param can be undefined
  - Recharts `ResponsiveContainer` must have explicit height; for horizontal bar charts use `data.length * rowHeight + padding` formula
  - Category breakdown bar chart uses `layout="vertical"` with `XAxis type="number" hide` and `YAxis type="category" dataKey="name"` for horizontal bars
  - Evidence display: use camelCase-to-human-readable conversion with regex `key.replace(/([A-Z])/g, " $1")` for JSON evidence keys
  - Indicator filtering/sorting is client-side since the risk-report API returns all indicators in one call
  - Prior submissions table uses `group-hover:opacity-100` pattern to reveal View link on row hover
  - Large risk gauge SVG uses viewBox="0 0 128 128" with r=54 for a bigger, more prominent display than the card version
  - Dashboard detail pages use `mx-auto max-w-5xl space-y-6 p-4 sm:p-6` layout pattern
---

## 2026-02-15 - US-036
- What was implemented: Document viewer tab and action buttons on the underwriter dashboard submission detail page
- Files changed:
  - src/app/dashboard/submissions/[id]/page.tsx (major rewrite: added tab navigation (Risk Report / Documents), document viewer with side-by-side layout, action bar with Approve/Request Info/Decline/Refer to SIU buttons, score override modal (SENIOR_UNDERWRITER/ADMIN only), false positive marking on individual indicators with justification)
  - src/app/api/submissions/[id]/indicators/[indicatorId]/route.ts (new: PATCH endpoint to mark/unmark indicators as false positive with justification, audit logging)
  - src/app/api/submissions/[id]/override-score/route.ts (new: PATCH endpoint to override risk score with new score 0-100, severity recalculation, justification required, SENIOR_UNDERWRITER/ADMIN only)
  - src/app/api/submissions/[id]/risk-report/route.ts (updated: returns ALL indicators including overridden ones with isOverridden/overriddenBy/overrideJustification fields; counts and breakdown use only active indicators)
  - src/app/api/submissions/[id]/route.ts (updated: document select now includes s3Key and extractedData for document viewer panel)
  - prd.json (marked US-036 as passes: true)
- **Learnings for future iterations:**
  - Risk report API should return ALL indicators (including overridden) for UI display, but score breakdown/counts should only use non-overridden ones
  - Tab navigation pattern: use `activeTab` state with border-b-2 styling for active tab indicator; conditionally render tab content
  - Sticky action bar: use `sticky bottom-0` with `bg-white/95 backdrop-blur-sm` for semi-transparent sticky footer that stays visible while scrolling
  - Score override needs separate API endpoint since it directly mutates submission.riskScore and submission.severity, bypassing the scoring engine
  - False positive toggle on indicators: PATCH to set isOverridden=true/false; API toggles state (if already overridden, un-overrides)
  - Document viewer side-by-side: `flex flex-col lg:flex-row` for responsive layout; PDF viewer is a placeholder since it requires S3 pre-signed URLs
  - Extracted data display: filter out `_metadata` key, use per-field confidence from metadata to highlight low-confidence fields in yellow
  - `useSession()` from next-auth/react provides user role for conditional UI (score override button visibility)
  - Action modals: decline requires justification (enforced by API), request-info accepts details text, approve/refer-to-siu are confirmation-only
---

## 2026-02-15 - US-037
- What was implemented: Underwriter dashboard threshold configuration page at /dashboard/config/thresholds with global threshold sliders, per-LOB overrides, score distribution histogram, preview mode, and save confirmation dialog
- Files changed:
  - src/app/dashboard/config/thresholds/page.tsx (new: full threshold config page with sliders for auto-approve/escalate thresholds, SIU referral toggle, per-LOB override table with add/remove, score distribution histogram via Recharts with threshold reference lines, preview mode showing re-routing impact on last 30 days, save confirmation dialog with before/after summary)
  - src/app/dashboard/config/page.tsx (replaced placeholder: config hub page with card links to thresholds, users, white-label, and API keys sub-pages)
  - prd.json (marked US-037 as passes: true)
- **Learnings for future iterations:**
  - Recharts `ReferenceLine` on bar charts: use `x` prop with exact data key value to place vertical reference lines on category (X) axis; for score buckets the key is the range string like "20-30"
  - Score distribution histogram: fetch submissions list and build client-side buckets (0-10, 10-20, ..., 90-100); color bars based on threshold zones (green < approve, yellow = review, red > escalate)
  - Preview mode pattern: toggle checkbox enables/disables preview panel; fetches `/api/config/thresholds/preview` with proposed values; shows impact summary (total, changed count, distribution comparison) and affected submissions table
  - Save confirmation dialog pattern: show before/after diff of changed values, warn about audit trail logging, require explicit confirmation
  - Config hub page: use card grid with icon + title + description linking to sub-pages; good pattern for settings areas with multiple sections
  - Slider inputs: HTML `<input type="range">` with `accent-*` for Tailwind color styling; display current value in a badge next to the label
  - Toggle switch: use `<button role="switch" aria-checked>` with translate-x transition for accessible toggle UI
  - Per-LOB overrides use available LOBs filtered by already-used ones to prevent duplicates
---

## 2026-02-15 - US-038
- What was implemented: User management page at /dashboard/config/users (ADMIN only) with user table, invite modal, and edit modal
- Files changed:
  - src/app/api/config/users/route.ts (new: GET for listing users with summary counts, POST for inviting new users with temp password generation, email notification, and audit logging)
  - src/app/api/config/users/[id]/route.ts (new: PATCH for editing user role and active status, self-deactivation prevention, audit logging)
  - src/app/dashboard/config/users/page.tsx (new: user management page with table view showing name, email, role badge, status, created date; invite user modal with email/name/role fields; edit user modal with role dropdown and active toggle; user count summary in header)
  - prd.json (marked US-038 as passes: true)
- **Learnings for future iterations:**
  - SIU case API: `withRole(["SIU_INVESTIGATOR", "ADMIN"])` for access; Prisma accessor is `prisma.sIUCase`; override field is `overriddenById` not `overriddenBy`
- User management API pattern: GET returns users + summary object with counts (total, active, activeUnderwriters) for the header display
  - Self-deactivation prevention: check `authUser.id === targetUser.id` server-side in PATCH; disable toggle client-side when `session?.user?.email === user.email`
  - Invite flow: generates temp password with `crypto.randomBytes(16)` + bcrypt hash; in production this would be replaced with a proper email invitation link with password reset
  - Audit logging: uses `USER_LOGIN` action type as closest match since the AuditAction type doesn't have a USER_MANAGEMENT variant — future iteration should extend AuditAction
  - Role colors: each role gets a distinct color for visual differentiation in the table (purple=Admin, blue=UW, indigo=Senior UW, red=SIU, amber=Compliance, green=Broker)
  - Modal pattern: fixed inset-0 z-50 overlay with max-w-md centered dialog, consistent with threshold config save dialog
---

## 2026-02-15 - US-039
- What was implemented: SIU case management API endpoints and case list page
- Files changed:
  - src/app/api/siu-cases/route.ts (new: GET /api/siu-cases with pagination, status filtering, includes submission details, assigned investigator, and indicator counts via groupBy)
  - src/app/api/siu-cases/[id]/route.ts (new: GET /api/siu-cases/:id for case detail with indicators; PATCH /api/siu-cases/:id for status update, adding notes/evidence, resolution; feedback to ML pipeline on CONFIRMED_FRAUD/FALSE_POSITIVE)
  - src/app/dashboard/siu-cases/page.tsx (replaced placeholder: full SIU case list page with table view, status filter, pagination, color-coded status badges, indicator counts, links to case detail page)
  - prd.json (marked US-039 as passes: true)
- **Learnings for future iterations:**
  - SIU case API uses `withRole(["SIU_INVESTIGATOR", "ADMIN"])` — only these roles should access SIU case data
  - Prisma model accessor for SIUCase is `prisma.sIUCase` (camelCase with leading lowercase 's')
  - Fraud indicator override field is `overriddenById` (not `overriddenBy` which is the relation name) — always check schema field names vs relation names
  - SIU case notes and evidence are JSON arrays — append to existing array by spreading `siuCase.notes as Array<...>` and adding new entry, then `JSON.parse(JSON.stringify())` for Prisma Json type
  - Terminal status changes (CONFIRMED_FRAUD, FALSE_POSITIVE) should feed back to FraudIndicator records via `updateMany` — marks all indicators as overridden for false positives
  - Indicator counts per case: use `prisma.fraudIndicator.groupBy` with `by: ["submissionId"]` for efficient batch count across multiple submissions
  - SIU case auto-creation happens in both routing-engine.ts and submissions/[id]/status/route.ts (manual referral) — the case list page shows cases from both sources
---

## 2026-02-15 - US-040
- What was implemented: SIU case detail page at /dashboard/siu-cases/:id with full investigation workflow
- Files changed:
  - src/app/dashboard/siu-cases/[id]/page.tsx (new: full case detail page with 4 tabs — Fraud Indicators, Evidence, Timeline, Notes — plus status update buttons, resolution workflow, evidence upload modal, SLA timer, and case header with submission link)
  - prd.json (marked US-040 as passes: true)
- **Learnings for future iterations:**
  - SIU case detail reuses indicator display patterns from submission detail page but without false-positive marking (that's managed at the case level via resolution)
  - Timeline is built client-side by merging case creation event, notes, evidence uploads, and resolution into a single sorted list — no separate timeline API needed
  - Status transitions are modeled as a map: OPEN → INVESTIGATING, INVESTIGATING → EVIDENCE_GATHERED/FALSE_POSITIVE/INCONCLUSIVE, EVIDENCE_GATHERED → CONFIRMED_FRAUD/FALSE_POSITIVE/INCONCLUSIVE
  - Terminal statuses (CONFIRMED_FRAUD, FALSE_POSITIVE, INCONCLUSIVE) hide the status update bar and add note/evidence buttons since the case is closed
  - SLA timer uses severity-based hours (CRITICAL=24h, HIGH=48h, MEDIUM=5d, LOW=10d) computed from case createdAt
  - Resolution text is required for CONFIRMED_FRAUD and FALSE_POSITIVE but optional for INCONCLUSIVE
  - Notes displayed in reverse chronological order (newest first); notes panel supports markdown text (rendered as pre-wrap)
  - Evidence upload uses a URL/reference approach rather than direct file upload to S3 — matches the existing PATCH API which accepts type/url/description
---

## 2026-02-15 - US-041
- What was implemented: SIU report generation with two report types — State DOI Fraud Referral and Internal Investigation Summary — as downloadable PDFs from the case detail page
- Files changed:
  - src/services/report-generation.ts (new: generateSIUReport function with DOI fraud referral and investigation summary PDF builders using pdfkit)
  - src/services/state-fraud-warnings.ts (new: state-mandated fraud warning text for all 50 states + DC, with extraction helper for state code from address)
  - src/app/api/siu-cases/[id]/reports/route.ts (new: POST /api/siu-cases/:id/reports — generates PDF, stores as evidence attachment on case, returns PDF download)
  - src/app/dashboard/siu-cases/[id]/page.tsx (updated: added "Generate Report" dropdown button in case header with DOI Fraud Referral and Investigation Summary options, download handler, report-type evidence entries show download button)
  - package.json (added pdfkit, @types/pdfkit)
  - prd.json (marked US-041 as passes: true)
- **Learnings for future iterations:**
  - pdfkit uses streaming API — wrap in Promise with `doc.on("data"/"end"/"error")` to collect chunks into Buffer
  - pdfkit built-in fonts: Helvetica, Helvetica-Bold, Courier, Times-Roman (no need to embed custom fonts for basic reports)
  - For NextResponse with binary data, use `new Uint8Array(buffer)` instead of raw Buffer to avoid TS type error with BodyInit
  - Prisma Json fields need `as unknown as T[]` double cast (not just `as T[]`) for TypeScript satisfaction
  - Reports stored as evidence entries on the SIU case with type "report" and a `reportType` field for re-generation/download
  - Click-outside dropdown pattern: `useRef` + `useEffect` with mousedown listener on document, check `ref.current.contains(target)`
  - State fraud warnings vary significantly by state; fall back to a generic warning when state code cannot be determined
  - DOI report sections: case info, subject info, risk assessment, detailed findings, evidence collected, resolution, fraud warning, certification
  - Investigation summary sections: case overview, subject details, risk assessment, all indicators (including overridden), timeline, evidence inventory, notes, resolution
---

## 2026-02-15 - US-042
- What was implemented: Portfolio analytics page with summary metrics, trend charts, severity distribution, and top indicators
- Files changed:
  - src/app/api/analytics/summary/route.ts (new: GET /api/analytics/summary — KPI metrics: total submissions, flagged/approved/declined/SIU rates, avg processing time, severity counts, top fraud indicators by groupBy)
  - src/app/api/analytics/trends/route.ts (new: GET /api/analytics/trends — time-series data with daily/weekly granularity, flagged vs clean breakdown)
  - src/app/dashboard/analytics/page.tsx (replaced placeholder: full analytics dashboard with 6 KPI cards, date range selector (7D/30D/90D/YTD), trend line chart via Recharts LineChart, severity distribution donut chart via PieChart, top fraud indicators table)
  - prd.json (marked US-042 as passes: true)
- **Learnings for future iterations:**
  - Analytics API pattern: fetch all submissions in date range, compute metrics in-memory for simplicity; for large datasets, switch to Prisma groupBy/aggregate queries
  - Date range handling: parse `range` param (7d/30d/90d/ytd/custom) with `from`/`to` custom params; default to 30 days
  - Trend granularity: auto-switch from daily to weekly when date range exceeds 30 days; use Monday-based week buckets via `getBucketKey()` helper
  - Recharts Tooltip `labelFormatter` expects `(label: ReactNode, ...) => ReactNode` — use `String(label)` cast instead of typing param as `string`
  - Recharts PieChart with donut: use `innerRadius` + `outerRadius` + `paddingAngle` on `Pie` component; `Cell` for per-slice colors
  - Top indicators: use `prisma.fraudIndicator.groupBy({ by: ["indicatorName", "severity", "category"] })` with `_count` and `orderBy` for efficient aggregation
  - KPI card grid: `grid-cols-2 lg:grid-cols-3 xl:grid-cols-6` for responsive layout from 2 to 6 columns
  - Processing time calculation: use `updatedAt - createdAt` for completed submissions (APPROVED, DECLINED, REFERRED_TO_SIU)
---

## 2026-02-15 - US-043
- What was implemented: Portfolio analytics: broker quality scores and compliance dashboard with CSV export
- Files changed:
  - src/app/api/analytics/brokers/route.ts (new: GET /api/analytics/brokers — broker quality leaderboard with per-broker aggregation, quality score computation, optional brokerId drill-down returning submissions/flagTypes/trend)
  - src/app/api/analytics/compliance/route.ts (new: GET /api/analytics/compliance — antifraud plan checklist, audit trail completeness, SIU reporting rate, fraud warning compliance, data retention status, estimated savings calculation)
  - src/app/dashboard/analytics/page.tsx (major rewrite: added tab navigation (Overview/Broker Quality/Compliance), broker leaderboard with click-to-drill-down detail view showing submission history + flag types + trend chart, compliance tab with estimated savings card + antifraud plan checklist + compliance meters + data retention status, Export CSV button on all data tables)
  - prd.json (marked US-043 as passes: true)
- **Learnings for future iterations:**
  - Analytics page tab pattern: use `activeTab` state with tab-specific data fetching in `fetchData()` — only fetch data for the active tab to avoid unnecessary API calls
  - Broker quality score formula: `cleanRate - (criticalCount * 5 penalty, max 30)` — penalizes brokers with frequent critical flags
  - Broker drill-down: pass `brokerId` query param to the same API endpoint; API returns different response shape (detail vs leaderboard) based on presence of brokerId
  - Compliance API: use `prisma.fraudIndicator.groupBy({ by: ["category"] })` to check which detection categories have been triggered (= implemented)
  - Prisma enum typing: `FraudIndicatorCategory[]` and `SIUCaseStatus[]` need explicit array type casts for `{ in: [...] }` filter queries
  - CSV export helper: `downloadCSV()` creates a Blob URL, triggers an anchor click, and revokes the URL — works client-side without server involvement
  - Estimated savings: `criticalFlaggedCount * avgFraudLossAmount` with configurable average (default $75K)
  - Compliance meters use color-coded progress bars: green (>=80%), yellow (>=50%), red (<50%)
---

## 2026-02-15 - US-044
- What was implemented: Audit trail viewer page at /dashboard/audit-trail with full filtering, search, expandable details, CSV export, and pagination
- Files changed:
  - src/app/dashboard/audit-trail/page.tsx (replaced placeholder: full audit trail page with table view, action type/user/date range/submission ID filters, free-text search across details JSON, expandable row details showing full JSON with structured rendering, CSV export, pagination with 50 entries per page, filter chips, empty state)
  - src/app/api/audit-logs/route.ts (updated: added free-text search via `string_contains` on details JSON field, added distinct users list in response for filter dropdown)
  - prd.json (marked US-044 as passes: true)
- **Learnings for future iterations:**
  - Prisma JSON field free-text search: use `{ string_contains: searchTerm }` filter on Json columns — works for substring matching within the serialized JSON
  - Distinct users for filter dropdown: use `prisma.auditLog.findMany({ distinct: ["userId"], select: { userId: true, user: ... } })` to get unique users who appear in audit logs without needing a separate users endpoint
  - Expandable table row pattern: use `<>` Fragment to render main `<tr>` + conditional expanded `<tr>` with `colSpan` for the detail panel; track `expandedId` state to toggle
  - Structured JSON rendering: recursive `renderValue()` function handles null, boolean, number, string, array, and object types with appropriate color coding and indentation
  - Filter chip pattern: small removable badges showing active filters with "Clear all" button; use `handleFilterChange()` wrapper to reset page to 1 on any filter change
  - Pagination helper: `generatePageNumbers()` creates a compact page list with ellipsis (1 ... 4 5 6 ... 20) for large page counts
  - Details summary: `summarizeDetails()` extracts key fields (reason, justification, status, action) for the truncated preview in the table row
---

## 2026-02-15 - US-045
- What was implemented: White-label settings configuration page at /dashboard/config/white-label (ADMIN only) with logo upload, color pickers, custom domain configuration, and legal/contact fields
- Files changed:
  - src/app/api/config/white-label/route.ts (new: GET for fetching current white-label config with defaults fallback, PUT for updating config fields with hex color validation, audit logging, upsert pattern)
  - src/app/api/config/white-label/logo/route.ts (new: POST for logo upload to S3 with file type/size validation, stores S3 URL in WhiteLabelConfig, audit logging)
  - src/app/dashboard/config/white-label/page.tsx (new: full white-label settings page with logo upload/preview, 3 color pickers with hex input and native color picker, live portal header preview, custom domain with CNAME instructions, custom email domain with SPF/DKIM setup guide, legal URLs and contact fields, sticky save bar with unsaved changes indicator, discard button)
  - prd.json (marked US-045 as passes: true)
- **Learnings for future iterations:**
  - AuthContext union type: use `ctx.auth.type === "user" ? (ctx.auth.user as AuthUser) : null` pattern — direct `ctx.auth.user` access fails TS since API key auth doesn't have a `user` property
  - Logo upload uses S3 key path `{tenantId}/branding/logo.{ext}` — different from document uploads which use `{tenantId}/{submissionId}/{documentId}/{filename}`
  - WhiteLabelConfig upsert: use `prisma.whiteLabelConfig.upsert({ where: { tenantId }, ... })` since config may not exist yet for the tenant
  - Color picker pattern: combine `<input type="color">` for visual picker with `<input type="text">` for hex input and a round color swatch preview
  - Live portal header preview: render a mock header/button bar with actual color values applied via inline `style` prop for immediate visual feedback
  - Reuses THRESHOLD_CHANGED audit action type since there's no dedicated WHITE_LABEL_CHANGED action in AuditAction union — future iteration could add specific action type
  - Sticky save bar pattern: `sticky bottom-0 z-10` with `bg-white/95 backdrop-blur-sm` for semi-transparent footer that persists while scrolling
---

## 2026-02-15 - US-046
- What was implemented: API key management page and OpenAPI documentation viewer at /dashboard/config/api (ADMIN only)
- Files changed:
  - src/app/api/config/api-keys/route.ts (new: GET /api/config/api-keys for listing keys with prefix-only display, POST for creating keys with hashed storage and one-time full key return)
  - src/app/api/config/api-keys/[id]/route.ts (new: PATCH /api/config/api-keys/:id for revoking keys via soft-delete)
  - src/app/api/docs/openapi.json/route.ts (new: GET /api/docs/openapi.json serving comprehensive OpenAPI 3.0.3 spec covering all public API endpoints)
  - src/app/dashboard/config/api/page.tsx (new: API key management page with tabbed view — API Keys tab with create/revoke/list, API Documentation tab with embedded swagger-ui-react viewer, sandbox toggle, one-time key reveal banner)
  - package.json (added swagger-ui-react, @types/swagger-ui-react)
  - prd.json (marked US-046 as passes: true)
- **Learnings for future iterations:**
  - API key generation: use `qsk_` prefix + 32 random bytes hex for identifiable keys; store SHA-256 hash via `hashApiKey()` from auth-middleware.ts
  - API key listing: never return full key or hash — use `key.substring(0, 8)` of the hash as prefix for display
  - One-time key reveal pattern: return full key only in POST creation response; store in component state with dismiss button and copy-to-clipboard
  - swagger-ui-react requires `dynamic(() => import("swagger-ui-react"), { ssr: false })` in Next.js to avoid SSR hydration issues
  - OpenAPI spec served as static JSON object from a route handler — no build step needed, easy to maintain inline
  - Reuses THRESHOLD_CHANGED audit action type for API key create/revoke since AuditAction union doesn't have API_KEY_CREATED/REVOKED variants
  - Sandbox toggle is a UI-only indicator in MVP — production would switch API key context between environments
---

## 2026-02-15 - US-047
- What was implemented: SOS entity verification service that checks business entities against Secretary of State records (mock API for MVP)
- Files changed:
  - src/services/verification-sos.ts (new: verifyEntity function with mock SOS API, 4 fraud checks — entity not found, inactive/dissolved, recent incorporation with mature ops, virtual office/PO box agent address)
  - prd.json (marked US-047 as passes: true)
- **Learnings for future iterations:**
  - Entity verification follows detection service pattern: main function fetches submission for tenantId, extracts entity info from ACORD 125 or entity docs, queries external API, runs parallel checks
  - Prisma FraudIndicator field is `recommendedAction` (camelCase), NOT `recommended_action` (snake_case) — Prisma auto-maps DB column names
  - Mock SOS API uses deterministic responses based on entity name keywords (phantom/shell → NOT_FOUND, dissolved/inactive/suspended → matching status) for testability
  - Entity info extraction cascades: ACORD 125 (businessName + physicalAddress) → entity docs (entityName + stateOfIncorporation)
  - State code extraction from address uses regex patterns: ", CA 90210" or ", CA," or trailing "CA"
  - Virtual office/PO box detection uses keyword matching: "po box", "virtual office", "mailbox", "pmb" etc.
  - Retry wrapper with exponential backoff (3 attempts, 1s/2s/4s delays) for API resilience; graceful fallback to manual verification on total failure
  - Mature operations check requires ALL three conditions: incorporated <2 years AND at least one maturity indicator (year established mismatch, high revenue >$1M, many employees >20)
---

## 2026-02-15 - US-048
- What was implemented: OFAC SDN screening and broker license verification services
- Files changed:
  - src/services/verification-ofac-broker.ts (new: screenOFAC function with fuzzy name matching via Levenshtein distance against mock SDN list; verifyBrokerLicense function with mock NIPR API and 3 checks — not found, expired/suspended/revoked, disciplinary history)
  - prd.json (marked US-048 as passes: true)
- **Learnings for future iterations:**
  - OFAC screening extracts ALL named entities/individuals from ACORD 125 (businessName, applicantName, dba), entity docs (entityName, registeredAgent, officers), and COI (insuredName, agentName) — deduplicates by uppercased name
  - Fuzzy name matching uses Levenshtein distance with normalized similarity score (0-1); default threshold 0.85 balances false positives vs false negatives
  - Mock OFAC SDN list uses keyword-triggered entries (e.g., "sanction test entity") for deterministic testing
  - Broker info extraction cascades: COI agentName → submission submitter with BROKER role; state inferred from ACORD 125 physical/mailing address
  - Mock NIPR API uses same keyword-based deterministic pattern as SOS mock (unlicensed → NOT_FOUND, expired → EXPIRED, suspended → SUSPENDED)
  - Both services follow verification-sos.ts patterns: findExtractedData helper, extractStateFromAddress helper, withRetry wrapper, category ENTITY_INTEL
  - Broker license checks are layered: NOT_FOUND (CRITICAL) → status check (CRITICAL) → disciplinary history (HIGH) — all three can trigger independently
---

## 2026-02-15 - US-049
- What was implemented: Address classification service and EIN validation with web presence checking
- Files changed:
  - src/services/verification-address-ein.ts (new: classifyAddress function with heuristic pattern matching for RESIDENTIAL/COMMERCIAL/VIRTUAL_OFFICE/PO_BOX; validateEIN function orchestrating 3 parallel checks — address classification, EIN timing, web presence for new entities)
  - prd.json (marked US-049 as passes: true)
- **Learnings for future iterations:**
  - Address classification uses layered heuristic: PO Box patterns (highest priority, 0.95 confidence) → virtual office keywords (0.8 confidence) → commercial vs residential pattern scoring → default commercial with low confidence (0.4)
  - classifyAddress is a pure function (no DB access) — exported separately for reuse in other services; validateEIN is the main entry point that orchestrates DB-aware checks
  - EIN timing check: extracts EIN from entity docs, looks up issuance date via mock API, compares against application date (from ACORD 125 effective date or submission createdAt fallback). Flag if <6 months
  - Web presence check only runs for entities incorporated <2 years — uses incorporation date from entity docs or yearEstablished from ACORD 125 as fallback
  - Mock APIs use deterministic keyword-based responses: EIN starting with "99" → 3 months old, "88" → 1 month old; entity names with "no web"/"nowebsite" → no web presence
  - Address classification checks both ACORD 125 addresses (physical + mailing) AND entity document registered agent address — creates separate FraudIndicator per flagged address
  - All checks run in parallel via Promise.all following the detection service pattern with category ENTITY_INTEL
---
